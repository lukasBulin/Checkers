#include "PlayerInput.h"
#include <iostream>
using namespace std;

SNextMove CPlayerInput::ProcessPlayerInput(const CCheckerBoard* board)
{
	//test code
	int tempVal;
	cin >> (tempVal);
	cin.clear();
	cin.ignore();

	return SNextMove();
}